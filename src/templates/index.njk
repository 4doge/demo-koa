<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <p>Hello world!</p>
  {{ username }}
  <ul>
  {% for category in categories %}
  <li>
  {{ category.name }} | {{ category.description }} | {{ category.viewsCount }} | {{ category._id }} <br>
    <ol>
      {% for subcategory in category.subcategories %}
      <li>{{ subcategory.name }} | {{ subcategory._id }} | {{ subcategory.description }}</li>
      {% endfor %}
    </ol>
  </li>
  {% endfor %}
  </ul>
</body>
</html>